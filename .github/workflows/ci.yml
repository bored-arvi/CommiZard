# This workflow runs a basic check on pushes and PRs

name: CI check

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["main"]

jobs:
  check:
    name: Run sanitizing checks

    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v5

      - name: Restore Cached Python
        id: cache-py
        uses: actions/cache@v4
        with:
          path: ~/.cache/deps
          key: ${{ runner.os }}-py-${{ hashFiles('pyproject.toml') }}

      - name: Restore Cached dependencies
        id: cache-dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/deps
          key: ${{ runner.os }}-deps-${{ hashFiles('pyproject.toml') }}

      - if: ${{ steps.cache-py.outputs.cache-hit != 'true' }}
        name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - if: ${{ steps.cache-dependencies.outputs.cache-hit != 'true' }}
        name: Install project with dev packages and Nox
        run: |
          pip install nox
          pip install .[dev]

